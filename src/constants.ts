// Shared UI constants (not data structures — kept out of types.ts).

// Width (logical px) of the embedded OpenCode webview opened on the right side
// of the office window. Must stay in sync with the Rust side in
// src-tauri/src/lib.rs (open_embedded_webview / EMBEDDED_WEBVIEW_WIDTH).
export const EMBEDDED_WEBVIEW_WIDTH = 740;
